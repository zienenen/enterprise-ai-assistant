export default function ArchivesPage() {
  return (
    <main className="min-h-screen px-8 py-6">
      <section className="mx-auto max-w-7xl rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
        <h1 className="text-2xl font-semibold text-slate-950">数据封存</h1>
        <p className="mt-3 text-sm text-slate-600">管理员封存和解封日期批次。封存后仍可查看和导出，但禁止写入。</p>
      </section>
    </main>
  );
}
